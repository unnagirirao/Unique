apiVersion: v1
kind: Service
metadata:
  namespace: unique
  labels:
    app: unique
  name: unique
spec:
  ports:
  
    - protocol: TCP
      port: 5634
      targetPort: 5634
   
   
  selector:
    app: unique
  type: ClusterIP
